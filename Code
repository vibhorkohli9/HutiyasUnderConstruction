// SPDX-License-Identifier: MIT (MIT is the least resctrictive lisence)
pragma solidity ^0.8.8; // ^ this symbol means that above that this version works

// contract is a keyword
contract SimpleStorage {
    //boolean, uint(positive whole no.), int, address, bytes
    bool hasFavoutiteNumber = true;
    uint256 favouriteNumber = 5; // uint8 to uint256
    string favouriteNumberInText = "Five";
    int256 favouriteInt = -5;
    address myAddress =   0x0c9bfbD17229453ACd5f09a1Ed59CFC644f98E6F;
    bytes32 favouriteBytes = "cat";


}
